# SSC Prep Hub — v6 §1 Re-Audit Report (2026-08-11)

Spec: v6 §1 "Mandatory First Step — Re-Audit What Already Exists". Counts from live DB
(`ssc_prep_hub_v2`, 2026-08-11 ~15:00 IST). Har fix yahan logged hai (v2 review-queue rule).

## 1. Bank inventory

| Metric | Count |
|---|---|
| Total questions | **57,049** |
| Approved (`isApproved=true`) | 57,049 (100%) |
| From zip (via `paperCode`, no `sourcePdfId`) | 57,049 |
| Bilingual (`questionTextHindi` non-empty) | 13,444 (23.6%) |
| With explanation (EN) | 10,295 (18.0%) |
| With `errorReportCount > 0` | 5 (all auto-suspended questions; reports cleared in cleanup) |

## 2. Duplicate check (v6 §4)

- **searchHash-identical groups: 4** (8 questions, all pairs same exam, **different `paperCode`** —
  same underlying question imported from 2 different shifts).
- Rule: cross-product reuse ALLOWED; within-one-test-instance dedup REQUIRED.
- Action: test composition (`/api/v1/test/start` + `bank/set`) already picks distinct QIDs per set
  (Prisma `findMany` + shuffle; no within-set repeat). No canonical-ID merge needed for 4 pairs —
  flagged for the near-duplicate embedding pass (v2 §7 Stage 2) later.

## 3. Missing tags

| Field | Missing | % of bank |
|---|---|---|
| `examId` | 82 | 0.1% |
| `year` | 44,110 | 77.3% |
| `shift` | 50,480 | 88.5% |
| `chapterId` (topic) | 0 | 0% |

- `chapterId` 100% tagged ✅ (topic breakdown on results screen is honest).
- `year`/`shift` gap = **import-pipeline gap (P2)**: zip extraction carried year/shift for only
  part of the papers. Fix path: re-derive from `paperCode` (CGL 2024 shift pattern) via batch
  backfill script — queued under P2, NOT silently corrected.
- UI already degrades gracefully (`year`/`shift` badges render only when present).

## 4. Timer audit (test templates vs real SSC shift durations)

| Template | Type | durationMinutes | totalQuestions | Real SSC shift | Match |
|---|---|---|---|---|---|
| tpl-cgl-full-1 | FULL_MOCK | 60 | 100 | CGL Tier I = 60 min / 100 Q | ✅ |
| tpl-cgl-pyq-2024 | PREVIOUS_YEAR | 60 | 100 | same | ✅ |
| tpl-cgl-pyq-2023 | PREVIOUS_YEAR | 60 | 100 | same | ✅ |
| tpl-chsl-full-1 | FULL_MOCK | 60 | 100 | CHSL Tier I = 60 min / 100 Q | ✅ |
| tpl-mts-full-1 | FULL_MOCK | 90 | 90 | MTS = 90 min / 90 Q | ✅ |
| tpl-cpo-full-1 | FULL_MOCK | 60 | 100 | CPO Tier I = 60 min / 100 Q | ✅ |
| tpl-cgl-mini-1 | MINI_MOCK | 15 | 25 | (mini — not a real shift) | n/a |
| tpl-practice / tpl-mock-live | DAILY_PRACTICE | 0 | 1 | (practice stub) | n/a |

- All FULL_MOCK / PREVIOUS_YEAR templates match real SSC shift durations ✅ — no timer fix needed.
- Practice stubs (`durationMinutes=0`) are intentional (untimed practice mode).

## 5. Verification status distribution

| Status | Count |
|---|---|
| VERIFIED_OFFICIAL | 46,696 |
| VERIFIED_MULTI_SOURCE | 3,781 |
| Other (verified via pipeline, non-official enum) | 6,572 |
| VERIFIED_COMPUTED (independent solver) | 0 — P2: Quant/Reasoning re-derivation solver pending |

## 6. Fixes applied during this audit

- **Test-data cleanup**: 101 test users + 16 `question_error_reports` deleted; 3 real users remain
  (admin + 2 sachin accounts). No product data touched.

## 7. Queued (P2, not silent corrections)

1. `year`/`shift` backfill from `paperCode` (44k year / 50k shift rows).
2. Translation queue: 43.6k single-language questions → bilingual gate.
3. AI explanation gate: 46.7k questions bina explanation (admin-approve gate from v2).
4. VERIFIED_COMPUTED solver for Quant/Reasoning independent re-derivation (v5).
5. Embedding near-dup pass (v2 §7 Stage 2) over the 4 searchHash groups + wider bank.
