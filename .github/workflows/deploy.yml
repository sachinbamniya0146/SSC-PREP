name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: read

concurrency:
  group: production-deploy
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      DEPLOY_DELAY_SECONDS: 1800
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate approved deploy host/IP
        env:
          TARGET_HOST: ${{ secrets.DEPLOY_HOST }}
          ALLOWED_HOSTS: ${{ secrets.DEPLOY_ALLOWED_HOSTS }}
        run: |
          if [ -z "${TARGET_HOST}" ]; then
            echo "❌ Missing secret DEPLOY_HOST"
            exit 1
          fi
          if [ -z "${ALLOWED_HOSTS}" ]; then
            echo "❌ Missing secret DEPLOY_ALLOWED_HOSTS (comma/newline-separated list of approved IPs/domains)"
            exit 1
          fi

          allowed="false"
          while IFS= read -r host; do
            host="$(echo "$host" | xargs)"
            [ -z "$host" ] && continue
            if [ "$host" = "${TARGET_HOST}" ]; then
              allowed="true"
              break
            fi
          done < <(echo "${ALLOWED_HOSTS}" | tr ',;' '\n')

          if [ "${allowed}" != "true" ]; then
            echo "❌ DEPLOY_HOST '${TARGET_HOST}' is not in DEPLOY_ALLOWED_HOSTS"
            exit 1
          fi
          echo "✅ Deploy host validated"

      - name: Delay auto deploy by 30 minutes
        if: github.event_name == 'push'
        run: |
          echo "⏳ Push detected. Waiting ${DEPLOY_DELAY_SECONDS}s before deployment..."
          sleep "${DEPLOY_DELAY_SECONDS}"
      
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}
      
      - name: Verify SSH connection
        run: |
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} "echo 'SSH connection successful' && hostname"
      
      - name: Copy production env file
        env:
          PRODUCTION_ENV: ${{ secrets.PRODUCTION_ENV }}
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} "
            cd /opt/ssc-prep-hub && 
            if [ ! -f .env ]; then
              echo 'Creating .env from template...'
              printf '%s' \"$PRODUCTION_ENV\" > .env
            fi
          "
      
      - name: Run deployment script with retry
        id: deploy
        run: |
          max_retries=3
          retry_count=0
          while [ $retry_count -lt $max_retries ]; do
            echo "🚀 Deployment attempt $((retry_count + 1))/$max_retries"
            if ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 -o ServerAliveInterval=30 -o ServerAliveCountMax=3 ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST}} "
              cd /opt/ssc-prep-hub &&
              chmod +x deploy.sh &&
              ./deploy.sh
            "; then
              echo "✅ Deployment succeeded"
              exit 0
            else
              retry_count=$((retry_count + 1))
              if [ $retry_count -lt $max_retries ]; then
                echo "⚠️ Deployment failed, retrying in 60 seconds..."
                sleep 60
              else
                echo "❌ All deployment attempts failed"
                exit 1
              fi
            fi
          done
        timeout-minutes: 80
      
      - name: Verify deployment with retry
        if: success()
        run: |
          max_retries=5
          retry_count=0
          while [ $retry_count -lt $max_retries ]; do
            echo "🔍 Verification attempt $((retry_count + 1))/$max_retries"
            if ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} "
              sleep 15 &&
              curl -sf -o /dev/null -w '%{http_code}' --max-time 30 https://sscprephub.in | grep -q '^200$' &&
              curl -sf -o /dev/null -w '%{http_code}' --max-time 30 https://sscprephub.in/api/v1/health | grep -q '^200$'
            "; then
              echo "✅ Deployment verification PASSED"
              exit 0
            else
              retry_count=$((retry_count + 1))
              if [ $retry_count -lt $max_retries ]; then
                echo "⚠️ Verification failed, retrying in 30 seconds..."
                sleep 30
              else
                echo "❌ All verification attempts failed"
                exit 1
              fi
            fi
          done
        timeout-minutes: 10

  notify:
    name: Notify on failure
    runs-on: ubuntu-latest
    needs: deploy
    if: failure()
    steps:
      - name: Send failure notification
        run: |
          echo "Deployment failed for commit ${{ github.sha }}"
          echo "Check GitHub Actions for details"
          # Add Slack/Discord/Email notification here if needed